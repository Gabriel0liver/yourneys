<main class="container">
  <section id="layout-profile">
    <img src="{{user.url}}" alt="">
    <h2>{{user.username}}</h2>
      <button type="submit" class="button add">Edit</button>
    <p> Description {{user.description}}</p>
  </section>
</main>

